<div id="<%= dom_id product %>">
  <p>
    <strong> <%= product.product_name %></strong>
  </p>
  <p>
    <% if  product.description.length < 20 %>
      <%= product.description %>
    <% else %>
      <%= product.description[0, 20] + "..." %>
    <% end %>
  </p>
</div>
